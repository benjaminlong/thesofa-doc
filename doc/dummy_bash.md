# Dummy Bash

```python
# Print Working directory
pwd

# List working directory
ls
ls -l (aliase `ll`)

# Change directory
cd path/to/new/dir

# Make directory, Remove directory
mkdir new_dir
rmdir new_dir

# Create empty file, remove file, move file
touch emptyfile.txt 
rm emtpyfile.txt
mv emtpyfile.txt new_emptyfile.txt

# Change Owndership, Change Mode
chmod -R 775 dir_name
chown -R user dir_name
```
